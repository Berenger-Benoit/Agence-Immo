<nav id="header" class="navbar navbar-expand-lg fixed-top">
    <a class="navbar-brand" href="{{ path('home') }}">Agence</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('property_list') }}">Liste des Biens</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Acheter</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Louer</a>
            </li>
            <li class="nav-item">
                <a id="nav-account" class="nav-link" href="{{path('app_login')}}">Mon compte
                <i class="fa fa-user" aria-hidden="true"></i></a>
            </li>
            {# <li class="nav-item">
                <a class="nav-link" href="{{path('app_register')}}">Créer compte</a>
            </li> #}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN')  %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_list') }}">Admin</a>
            </li>
            {% endif %}
        </ul>

    </div>
</nav>